# Root-level ignores
.DS_Store
*.log
*.swp
*.swo
*~

# Environment files
.env
.env.local
.env.*.local
.env.production
.env.development

# Sensitive files - NEVER COMMIT THESE
relayer-keypair.json
relayer/*-keypair.json
relayer/relayer-keypair.json
*-keypair.json
*.key
*.pem
*.p12
*.pfx
secrets/
keys/
keys/**/*.json
*.secret
*.private

# Utility and development scripts
finalize_comp_def.*
check_cluster_offset.*
integration.test.js
test-*.js
test-*.ts

# IDE
.vscode/
.idea/
*.iml

# OS
Thumbs.db
.DS_Store

# Build artifacts (subdirectories handle their own)
dist/
build/
*.so

# Temporary files
*.tmp
*.bak
*.backup

# Test files (keep structure, ignore generated)
coverage/
*.test.js.map
*.spec.js.map

# Documentation (keep research docs, ignore generated)
# Keep: MERKLE_RESEARCH.md, MERKLE_IMPLEMENTATION_PLAN.md
# Ignore: any auto-generated docs

# Node modules (handled by subdirectory gitignores, but good to have here too)
node_modules/

# WSL specific
.wslconfig

# Yarn
.yarn/
.yarnrc.yml
yarn-error.log

# Package manager locks (keep npm, ignore others for now)
# package-lock.json - KEEP
# yarn.lock - IGNORE (if using npm)
# pnpm-lock.yaml - IGNORE